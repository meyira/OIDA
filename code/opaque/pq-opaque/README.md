If opaque-server-nrorpf does not link libnr-oprf-so, then add it to LD_LIBRARY_PATH \
TODO: KAT gives wrong results on authentication, but key exchange is correct: maybe init_classgroup is the problem?
